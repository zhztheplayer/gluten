VeloxColumnarToRow
  WholeStageCodegenTransformer (3)
    SortExecTransformer [l_returnflag,l_linestatus]
      InputIteratorTransformer
        InputAdapter
          ColumnarExchange [l_returnflag,l_linestatus] #1
            VeloxResizeBatches
              WholeStageCodegenTransformer (2)
                ProjectExecTransformer [l_returnflag,l_linestatus,sum(l_quantity),sum(l_extendedprice),sum((l_extendedprice * (1 - l_discount))),sum(((l_extendedprice * (1 - l_discount)) * (1 + l_tax))),avg(UnscaledValue(l_quantity)),avg(UnscaledValue(l_extendedprice)),avg(UnscaledValue(l_discount)),count(1)]
                  RegularHashAggregateExecTransformer [l_returnflag,l_linestatus,sum,isEmpty,sum,isEmpty,sum,isEmpty,sum,isEmpty,sum,count,sum,count,sum,count,count] [sum(l_quantity),sum(l_extendedprice),sum((l_extendedprice * (1 - l_discount))),sum(((l_extendedprice * (1 - l_discount)) * (1 + l_tax))),avg(UnscaledValue(l_quantity)),avg(UnscaledValue(l_extendedprice)),avg(UnscaledValue(l_discount)),count(1),sum,isEmpty,sum,isEmpty,sum,isEmpty,sum,isEmpty,sum,count,sum,count,sum,count,count]
                    InputIteratorTransformer
                      InputAdapter
                        ColumnarExchange [l_returnflag,l_linestatus,sum,isEmpty,sum,isEmpty,sum,isEmpty,sum,isEmpty,sum,count,sum,count,sum,count,count] #2
                          VeloxResizeBatches
                            WholeStageCodegenTransformer (1)
                              ProjectExecTransformer [l_returnflag,l_linestatus,sum,isEmpty,sum,isEmpty,sum,isEmpty,sum,isEmpty,sum,count,sum,count,sum,count,count]
                                FlushableHashAggregateExecTransformer [l_returnflag,l_linestatus,l_quantity,l_extendedprice,_pre_x,_pre_x,_pre_x,_pre_x,_pre_x] [sum,isEmpty,sum,isEmpty,sum,isEmpty,sum,isEmpty,sum,count,sum,count,sum,count,count,sum,isEmpty,sum,isEmpty,sum,isEmpty,sum,isEmpty,sum,count,sum,count,sum,count,count,sum,isEmpty,sum,isEmpty,sum,count,sum,count,sum,count]
                                  ProjectExecTransformer [l_quantity,l_extendedprice,l_returnflag,l_linestatus,l_discount,l_tax]
                                    FilterExecTransformer [l_shipdate]
                                      FileSourceScanExecTransformer parquet spark_catalog.default.lineitem [l_quantity,l_extendedprice,l_discount,l_tax,l_returnflag,l_linestatus,l_shipdate]
