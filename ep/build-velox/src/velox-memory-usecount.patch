diff --git a/velox/common/caching/AsyncDataCache.h b/velox/common/caching/AsyncDataCache.h
index 8d61e2a0d..6ace12efb 100644
--- a/velox/common/caching/AsyncDataCache.h
+++ b/velox/common/caching/AsyncDataCache.h
@@ -471,6 +471,10 @@ class CoalescedLoad {
   /// storage.
   virtual bool isSsdLoad() const = 0;
 
+  virtual memory::MemoryPool* memoryPool() const {
+    return nullptr;
+  }
+
  protected:
   // Makes entries for 'keys_' and loads their content. Elements of 'keys_' that
   // are already loaded or loading are expected to be left out. The returned
diff --git a/velox/common/memory/MemoryPool.cpp b/velox/common/memory/MemoryPool.cpp
index 66ba6804c..fcfacea21 100644
--- a/velox/common/memory/MemoryPool.cpp
+++ b/velox/common/memory/MemoryPool.cpp
@@ -70,6 +70,7 @@ struct MemoryUsage {
   uint64_t currentUsage;
   uint64_t reservedUsage;
   uint64_t peakUsage;
+  int64_t useCount;
 
   bool operator>(const MemoryUsage& other) const {
     return std::tie(reservedUsage, currentUsage, peakUsage, name) >
@@ -82,11 +83,12 @@ struct MemoryUsage {
 
   std::string toString() const {
     return fmt::format(
-        "{} usage {} reserved {} peak {}",
+        "{} usage {} reserved {} peak {} useCount {}",
         name,
         succinctBytes(currentUsage),
         succinctBytes(reservedUsage),
-        succinctBytes(peakUsage));
+        succinctBytes(peakUsage),
+        useCount);
   }
 };
 
@@ -124,11 +126,13 @@ void treeMemoryUsageVisitor(
   if (stats.empty() && skipEmptyPool) {
     return;
   }
+  const auto self = pool->shared_from_this();
   const MemoryUsage usage{
       .name = pool->name(),
       .currentUsage = stats.usedBytes,
       .reservedUsage = stats.reservedBytes,
       .peakUsage = stats.peakBytes,
+      .useCount = static_cast<int64_t>(self.use_count() - 1),
   };
   out << std::string(indent, ' ') << usage.toString() << "\n";
 
@@ -1042,11 +1046,13 @@ std::string MemoryPoolImpl::treeMemoryUsage(bool skipEmptyPool) const {
   {
     std::lock_guard<std::mutex> l(mutex_);
     const Stats stats = statsLocked();
+    const auto self = shared_from_this();
     const MemoryUsage usage{
         .name = name(),
         .currentUsage = stats.usedBytes,
         .reservedUsage = stats.reservedBytes,
-        .peakUsage = stats.peakBytes};
+        .peakUsage = stats.peakBytes,
+        .useCount = static_cast<int64_t>(self.use_count() - 1)};
     out << usage.toString() << "\n";
   }
 
diff --git a/velox/dwio/common/DirectBufferedInput.cpp b/velox/dwio/common/DirectBufferedInput.cpp
index 45d148c32..340f900cd 100644
--- a/velox/dwio/common/DirectBufferedInput.cpp
+++ b/velox/dwio/common/DirectBufferedInput.cpp
@@ -20,6 +20,12 @@
 #include "velox/common/testutil/TestValue.h"
 #include "velox/dwio/common/DirectInputStream.h"
 
+#include <atomic>
+#include <mutex>
+#include <sstream>
+#include <thread>
+#include <unordered_map>
+
 DECLARE_int32(cache_prefetch_min_pct);
 
 using ::facebook::velox::common::Region;
@@ -31,6 +37,207 @@ using cache::CoalescedLoad;
 using cache::ScanTracker;
 using cache::TrackingId;
 
+namespace {
+
+bool shouldLogTableScanPool(const memory::MemoryPool* pool);
+
+std::mutex& tableScanPoolThreadOwnersMutex() {
+  static std::mutex mutex;
+  return mutex;
+}
+
+auto& tableScanPoolThreadOwners() {
+  static std::unordered_map<std::string, std::unordered_map<std::string, int32_t>>
+      owners;
+  return owners;
+}
+
+std::string currentThreadLabel() {
+  std::ostringstream out;
+  out << "tid=" << std::this_thread::get_id();
+  return out.str();
+}
+
+uint64_t nextAsyncLoadHolderId() {
+  static std::atomic<uint64_t> nextId{1};
+  return nextId.fetch_add(1, std::memory_order_relaxed);
+}
+
+std::string formatThreadOwnersLocked(std::string_view poolName) {
+  const auto poolIt = tableScanPoolThreadOwners().find(std::string(poolName));
+  if (poolIt == tableScanPoolThreadOwners().end() || poolIt->second.empty()) {
+    return "[]";
+  }
+
+  std::ostringstream out;
+  out << "[";
+  bool first = true;
+  for (const auto& [threadName, count] : poolIt->second) {
+    if (!first) {
+      out << ", ";
+    }
+    first = false;
+    out << threadName << ":" << count;
+  }
+  out << "]";
+  return out.str();
+}
+
+void trackAsyncLoadHolderRefChange(
+    const char* where,
+    const std::shared_ptr<memory::MemoryPool>& pool,
+    std::string_view threadName,
+    int32_t delta,
+    uint64_t holderId,
+    const void* holder) {
+  if (!shouldLogTableScanPool(pool.get())) {
+    return;
+  }
+
+  std::string owners;
+  {
+    std::lock_guard<std::mutex> lock(tableScanPoolThreadOwnersMutex());
+    auto& poolOwners = tableScanPoolThreadOwners()[pool->name()];
+    const auto threadKey = std::string(threadName);
+    auto it = poolOwners.find(threadKey);
+    if (it == poolOwners.end()) {
+      VELOX_CHECK_GE(
+          delta,
+          0,
+          "AsyncLoadHolder tracking underflow for pool {} thread {} delta {}",
+          pool->name(),
+          threadName,
+          delta);
+      it = poolOwners.emplace(threadKey, 0).first;
+    }
+    auto& count = it->second;
+    count += delta;
+    VELOX_CHECK_GE(
+        count,
+        0,
+        "AsyncLoadHolder tracking underflow for pool {} thread {} count {} delta {}",
+        pool->name(),
+        threadName,
+        count,
+        delta);
+    if (count == 0) {
+      poolOwners.erase(it);
+    }
+    if (poolOwners.empty()) {
+      tableScanPoolThreadOwners().erase(pool->name());
+    }
+    owners = formatThreadOwnersLocked(pool->name());
+  }
+
+  LOG(WARNING) << where << " pool=" << pool->name()
+               << " poolUseCount=" << pool.use_count()
+               << " delta=" << delta
+               << " holderId=" << holderId
+               << " holder=" << holder
+               << " thread=" << threadName
+               << " ownerThreads=" << owners;
+}
+
+bool shouldLogTableScanPool(const memory::MemoryPool* pool) {
+  return pool != nullptr &&
+      pool->name().find("TableScan#tspool") != std::string::npos;
+}
+
+} // namespace
+
+void debugTrackAsyncLoadHolderRefDecrement(
+    const std::shared_ptr<memory::MemoryPool>& pool) {
+  trackAsyncLoadHolderRefChange(
+      "AsyncLoadHolder dtor ref decrement",
+      pool,
+      currentThreadLabel(),
+      -1,
+      0,
+      nullptr);
+}
+
+void debugTrackAsyncLoadHolderRefChangeForThread(
+    const char* where,
+    const std::shared_ptr<memory::MemoryPool>& pool,
+    std::string_view threadName,
+    int32_t delta,
+    uint64_t holderId,
+    const void* holder) {
+  trackAsyncLoadHolderRefChange(
+      where, pool, threadName, delta, holderId, holder);
+}
+
+DirectBufferedInput::AsyncLoadHolder::AsyncLoadHolder(
+    std::shared_ptr<cache::CoalescedLoad> load,
+    std::shared_ptr<memory::MemoryPool> pool)
+    : load(std::move(load)),
+      pool(std::move(pool)),
+      holderId(nextAsyncLoadHolderId()),
+      ownerThread(currentThreadLabel()) {
+  if (this->pool != nullptr) {
+    debugTrackAsyncLoadHolderRefChangeForThread(
+        "AsyncLoadHolder ctor ref increment",
+        this->pool,
+        ownerThread,
+        1,
+        holderId,
+        this);
+  }
+}
+
+DirectBufferedInput::AsyncLoadHolder::AsyncLoadHolder(
+    AsyncLoadHolder&& other) noexcept
+    : load(std::move(other.load)),
+      pool(std::move(other.pool)),
+      holderId(other.holderId),
+      ownerThread(currentThreadLabel()) {
+  if (pool != nullptr) {
+    debugTrackAsyncLoadHolderRefChangeForThread(
+        "AsyncLoadHolder move ref decrement",
+        pool,
+        other.ownerThread,
+        -1,
+        holderId,
+        &other);
+    debugTrackAsyncLoadHolderRefChangeForThread(
+        "AsyncLoadHolder move ref increment",
+        pool,
+        ownerThread,
+        1,
+        holderId,
+        this);
+  }
+  LOG(WARNING) << "AsyncLoadHolder move holderId=" << holderId
+               << " from=" << static_cast<const void*>(&other)
+               << " to=" << static_cast<const void*>(this)
+               << " pool="
+               << (pool == nullptr ? std::string("null") : pool->name())
+               << " oldThread=" << other.ownerThread
+               << " newThread=" << ownerThread;
+  other.holderId = 0;
+  other.ownerThread.clear();
+}
+
+DirectBufferedInput::AsyncLoadHolder::~AsyncLoadHolder() {
+  // Release the load reference before the memory pool reference.
+  // This is to make sure the memory pool is not destroyed before we free up
+  // the allocated buffers.
+  // This is to handle the case that the associated task has already
+  // destroyed before the async load is done. The async load holds
+  // the last reference to the memory pool in that case.
+  load.reset();
+  if (pool != nullptr) {
+    debugTrackAsyncLoadHolderRefChangeForThread(
+        "AsyncLoadHolder dtor ref decrement",
+        pool,
+        ownerThread,
+        -1,
+        holderId,
+        this);
+  }
+  pool.reset();
+}
+
 std::unique_ptr<SeekableInputStream> DirectBufferedInput::enqueue(
     Region region,
     const StreamIdentifier* sid = nullptr) {
@@ -208,6 +415,8 @@ void DirectBufferedInput::readRegion(
   });
 }
 
+DirectCoalescedLoad::~DirectCoalescedLoad() = default;
+
 void DirectBufferedInput::readRegions(
     const std::vector<LoadRequest*>& requests,
     bool prefetch,
@@ -225,9 +434,18 @@ void DirectBufferedInput::readRegions(
     for (auto i = 0; i < coalescedLoads_.size(); ++i) {
       auto& load = coalescedLoads_[i];
       if (load->state() == CoalescedLoad::State::kPlanned) {
-        AsyncLoadHolder loadHolder{
-            .load = load, .pool = pool_->shared_from_this()};
+        auto thisPool = pool_->shared_from_this();
+        AsyncLoadHolder loadHolder{load, std::move(thisPool)};
+        LOG(WARNING) << "AsyncLoadHolder submit executor=" << static_cast<const void*>(executor_)
+                     << " holderId=" << loadHolder.holderId
+                     << " holder=" << static_cast<const void*>(&loadHolder)
+                     << " pool="
+                     << (loadHolder.pool == nullptr ? std::string("null") : loadHolder.pool->name());
         executor_->add([asyncLoad = std::move(loadHolder)]() {
+          LOG(WARNING) << "AsyncLoadHolder run holderId=" << asyncLoad.holderId
+                       << " holder=" << static_cast<const void*>(&asyncLoad)
+                       << " pool="
+                       << (asyncLoad.pool == nullptr ? std::string("null") : asyncLoad.pool->name());
           process::TraceContext trace("Read Ahead");
           VELOX_CHECK_NOT_NULL(asyncLoad.load);
           asyncLoad.load->loadOrFuture(nullptr);
diff --git a/velox/dwio/common/DirectBufferedInput.h b/velox/dwio/common/DirectBufferedInput.h
index c23e104e9..2c398c0c1 100644
--- a/velox/dwio/common/DirectBufferedInput.h
+++ b/velox/dwio/common/DirectBufferedInput.h
@@ -30,6 +30,16 @@
 
 namespace facebook::velox::dwio::common {
 
+void debugTrackAsyncLoadHolderRefDecrement(
+    const std::shared_ptr<memory::MemoryPool>& pool);
+void debugTrackAsyncLoadHolderRefChangeForThread(
+    const char* where,
+    const std::shared_ptr<memory::MemoryPool>& pool,
+    std::string_view threadName,
+    int32_t delta,
+    uint64_t holderId,
+    const void* holder);
+
 struct LoadRequest {
   LoadRequest() = default;
   LoadRequest(velox::common::Region& _region, cache::TrackingId _trackingId)
@@ -92,6 +102,10 @@ class DirectCoalescedLoad : public cache::CoalescedLoad {
     return false;
   }
 
+  memory::MemoryPool* memoryPool() const override {
+    return pool_;
+  }
+
   /// Returns the buffer for 'region' in either 'data' or 'tinyData'. 'region'
   /// must match a region given to DirectBufferedInput::enqueue().
   int32_t
@@ -109,6 +123,8 @@ class DirectCoalescedLoad : public cache::CoalescedLoad {
     return size;
   }
 
+  ~DirectCoalescedLoad() override;
+
  private:
   const std::shared_ptr<IoStatistics> ioStatistics_;
   const std::shared_ptr<velox::IoStats> ioStats_;
@@ -295,19 +311,20 @@ class DirectBufferedInput : public BufferedInput {
   // Holds the reference on the memory pool for async load in case of early task
   // terminate.
   struct AsyncLoadHolder {
+    AsyncLoadHolder(
+        std::shared_ptr<cache::CoalescedLoad> load,
+        std::shared_ptr<memory::MemoryPool> pool);
+    AsyncLoadHolder(AsyncLoadHolder&& other) noexcept;
+    AsyncLoadHolder(const AsyncLoadHolder&) = delete;
+    AsyncLoadHolder& operator=(const AsyncLoadHolder&) = delete;
+    AsyncLoadHolder& operator=(AsyncLoadHolder&&) = delete;
+
     std::shared_ptr<cache::CoalescedLoad> load;
     std::shared_ptr<memory::MemoryPool> pool;
+    uint64_t holderId{0};
+    std::string ownerThread;
 
-    ~AsyncLoadHolder() {
-      // Release the load reference before the memory pool reference.
-      // This is to make sure the memory pool is not destroyed before we free up
-      // the allocated buffers.
-      // This is to handle the case that the associated task has already
-      // destroyed before the async load is done. The async load holds
-      // the last reference to the memory pool in that case.
-      load.reset();
-      pool.reset();
-    }
+    ~AsyncLoadHolder();
   };
 
   // Coalesced loads spanning multiple streams in one IO.
diff --git a/velox/exec/Task.cpp b/velox/exec/Task.cpp
index fce8c7eb2..0d98bbd05 100644
--- a/velox/exec/Task.cpp
+++ b/velox/exec/Task.cpp
@@ -18,6 +18,7 @@
 #include <boost/lexical_cast.hpp>
 #include <boost/uuid/uuid_generators.hpp>
 #include <boost/uuid/uuid_io.hpp>
+#include <atomic>
 #include <string>
 
 #include "velox/common/base/Counters.h"
@@ -45,6 +46,26 @@ using facebook::velox::common::testutil::TestValue;
 namespace facebook::velox::exec {
 namespace {
 
+std::atomic<uint64_t> tableScanPoolDebugId{0};
+
+std::string maybeAppendTableScanPoolDebugId(const std::string& poolName) {
+  if (poolName.find(".TableScan") == std::string::npos) {
+    return poolName;
+  }
+  return fmt::format(
+      "{}#tspool.{}", poolName, tableScanPoolDebugId.fetch_add(1));
+}
+
+std::string maybeAppendTableScanConnectorPoolDebugId(
+    const std::string& poolName,
+    const std::string& operatorType) {
+  if (operatorType != "TableScan") {
+    return poolName;
+  }
+  return fmt::format(
+      "{}#tsconnector.{}", poolName, tableScanPoolDebugId.fetch_add(1));
+}
+
 // RAII helper class to satisfy given promises and notify listeners of an event
 // connected to the promises outside of the mutex that guards the promises.
 // Inactive on creation. Must be activated explicitly by calling 'activate'.
@@ -769,9 +790,9 @@ velox::memory::MemoryPool* Task::addOperatorPool(
   } else {
     nodePool = getOrAddNodePool(planNodeId);
   }
-  childPools_.push_back(nodePool->addLeafChild(
+  childPools_.push_back(nodePool->addLeafChild(maybeAppendTableScanPoolDebugId(
       fmt::format(
-          "op.{}.{}.{}.{}", planNodeId, pipelineId, driverId, operatorType)));
+          "op.{}.{}.{}.{}", planNodeId, pipelineId, driverId, operatorType))));
   return childPools_.back().get();
 }
 
@@ -783,13 +804,15 @@ velox::memory::MemoryPool* Task::addConnectorPoolLocked(
     const std::string& connectorId) {
   auto* nodePool = getOrAddNodePool(planNodeId);
   childPools_.push_back(nodePool->addAggregateChild(
-      fmt::format(
-          "op.{}.{}.{}.{}.{}",
-          planNodeId,
-          pipelineId,
-          driverId,
-          operatorType,
-          connectorId)));
+      maybeAppendTableScanConnectorPoolDebugId(
+          fmt::format(
+              "op.{}.{}.{}.{}.{}",
+              planNodeId,
+              pipelineId,
+              driverId,
+              operatorType,
+              connectorId),
+          operatorType)));
   return childPools_.back().get();
 }
 
